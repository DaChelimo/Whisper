<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.App" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="CropImageTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimary">@color/dark_blue</item>
        <item name="android:colorSecondary" tools:targetApi="n_mr1">@color/dark_blue</item>

        <item name="android:textColor">@color/black</item>
    </style>


    <style name="App.Splash" parent="Theme.SplashScreen">
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
     drawable. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen</item>
        <!-- Set the theme of the Activity that follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.App</item>
        <!-- Set a white background behind the splash screen icon. -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <!-- Set the splash screen background to white -->
        <item name="windowSplashScreenBackground">@android:color/white</item>
    </style>
</resources>